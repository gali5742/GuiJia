《周易》经文数据目录

龟甲 v13.29.0 会优先读取 ./data/iching.json。
若该文件不存在，则依次使用浏览器缓存和既有远程来源。

当前发布包尚未内置一份由本项目再次核定的完整 64 卦 JSON；
这是资料本地化的剩余项目，不影响排盘、纳甲、世应、用神等核心计算。

后续核定完成后保存为：
    data/iching.json

数据结构需保持加载器当前支持的数组格式，并至少包含 64 条记录。
