# 龟甲 v13.39.3 升级说明

本版继续收束八字“详细分析”页，不改排盘算法、原局总览内容、流年流月、六爻或 CSS。

主要变化：

- 重做“干支关系网络”主视图：不再把“天干关系 / 地支关系 / 半合与同方”等分类并列铺开。
- 同一对柱位存在多种关系时先合并，例如“年时两柱关系重叠”。
- 同一干支反复作用于多个柱位时合并为一条主线，例如“日支卯形成重复六破”“时干壬形成重复相冲”。
- 其余关系再按共同节点组织；只有无法继续合并的单一关系才单独呈现。
- 全部原始干支关系仍保留在“查看全部干支关系”折叠项中，供核对。
- “复制分析上下文”从原局总览移到八字详细分析页顶部；复制内容格式不变。

验证：70 passed / 0 failed；另做 50,000 组随机八字详细分析关系合成，0 异常；JS 语法与 source dependency 检查通过；CSS 未修改。

补丁工作区没有正式仓库中的 `vendor/vue.global.prod.js` 与 `vendor/lunar.js`，因此 lunar integration 在这里按设计 skip，最终静态资源检查也会在 vendor 文件存在性处停止。覆盖到正式仓库后请照常运行：

```powershell
npm test
npm run predeploy
git diff --check
```
