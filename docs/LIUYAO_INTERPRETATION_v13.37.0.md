# v13.37.0 · 六爻结构解读 v1 审计

## 架构

新增 `js/liuyao-interpretation.js`，解释层只消费 `liuyao-core` 已计算的结构化结果，不重新起卦、不重算纳甲，也不从 UI 中文文案反解析机器语义。

数据链：

`起卦 / 装卦 -> liuyao-core 结构事实 -> 当前用神 -> liuyao-interpretation 命题合成 -> UI / 复制上下文`

## v1 命题

- 用神日月与空破状态
- 用神动静与动变（仅有明确动变时单列）
- 元神 / 忌神 / 仇神的明爻分布
- 世应位置关系
- 六合六冲、动变三合、反吟伏吟等全卦结构

应期候选仍由独立模块展示，避免与总览已有应期卡重复。

## UI 约束

没有新增 CSS。六爻直接复用八字结构解读现有样式和通用折叠组件，因此两套解读保持同一视觉语言。

## 测试

- 现有 + 新增自动测试：50 / 50 通过。
- 4096 种六爻输入解释压力测试：
  - exceptions: 0
  - judgments: 3—5
  - empty titles: 0
  - duplicate ids: 0
  - machine code leaks: 0
